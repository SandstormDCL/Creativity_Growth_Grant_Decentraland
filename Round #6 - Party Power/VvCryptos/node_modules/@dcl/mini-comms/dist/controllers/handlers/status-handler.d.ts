import { HandlerContextWithPath } from '../../types';
export declare function statusHandler(context: Pick<HandlerContextWithPath<'metrics' | 'config', '/status'>, 'url' | 'components'>): Promise<{
    body: {
        commitHash: string | undefined;
    };
}>;
