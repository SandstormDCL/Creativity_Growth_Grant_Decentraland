import { WebSocket } from 'ws';
import { AppComponents } from '../types';
export declare function handleSocketLinearProtocol(components: Pick<AppComponents, 'rooms' | 'logs' | 'ethereumProvider'>, socket: WebSocket, room: string): Promise<void>;
