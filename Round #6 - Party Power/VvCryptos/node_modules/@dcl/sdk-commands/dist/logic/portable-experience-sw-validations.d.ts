/// <reference types="node" />
import { Scene } from '@dcl/schemas';
import { CliComponents } from '../components';
export interface IFile {
    path: string;
    content: Buffer;
    size: number;
}
export declare const SMART_WEARABLE_FILE = "wearable.json";
/**
 * Composes the path to the `scene.json` file based on the provided path.
 * @param projectRoot The path to the directory containing the scene file.
 */
export declare function getSmartWearableFile(projectRoot: string): string;
export declare function assertValidSmartWearable(scene: Scene): boolean;
/**
 * Get valid Scene JSON
 */
export declare function getValidWearableJson(components: Pick<CliComponents, 'fs' | 'logger'>, projectRoot: string): Promise<Scene>;
