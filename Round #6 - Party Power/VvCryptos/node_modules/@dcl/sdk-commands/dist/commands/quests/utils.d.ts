import { Router } from '@well-known-components/http-server';
import { CreateQuest, QuestLinkerActionType } from './types';
import { CliComponents } from '../../components';
import { dAppOptions } from '../../run-dapp';
export declare function executeSubcommand(components: CliComponents, linkerOps: Omit<dAppOptions, 'uri'>, commandData: {
    url: string;
    method: 'GET' | 'POST' | 'PUT' | 'DELETE';
    metadata: Record<any, any>;
    actionType: QuestLinkerActionType;
    extraData?: {
        questName?: string;
        questId?: string;
        createQuest?: CreateQuest;
    };
}, commandCallback: (authchainHeaders: Record<string, string>) => Promise<void>): Promise<void>;
export declare const urlRegex: RegExp;
export declare const createQuestByPrompting: (_components: Pick<CliComponents, 'logger'>) => Promise<CreateQuest | null>;
export declare const setUpManager: (components: Pick<CliComponents, 'fs' | 'logger' | 'fetch' | 'config'>) => {
    router: Router<{}>;
};
