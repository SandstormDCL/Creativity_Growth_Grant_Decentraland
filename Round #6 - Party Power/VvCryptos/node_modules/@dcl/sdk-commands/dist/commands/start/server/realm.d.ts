import { PreviewComponents } from '../types';
import { Router } from '@well-known-components/http-server';
/**
 * This module handles the BFF mock and communications server for the preview mode.
 * It runs using @dcl/mini-comms implementing RFC-5
 */
export declare function setupRealmAndComms(components: PreviewComponents, router: Router<PreviewComponents>, localSceneParcels?: string[]): void;
