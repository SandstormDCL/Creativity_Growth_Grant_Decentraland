import { DataLayerContext } from '@dcl/inspector';
import { RpcServer } from '@dcl/rpc';
import { CliComponents } from '../../../components';
export type DataLayer = {
    rpcServer: RpcServer<DataLayerContext>;
    context: DataLayerContext;
};
export declare function createDataLayer(components: Pick<CliComponents, 'fs' | 'logger'>): Promise<DataLayer>;
