import { CliComponents } from '../../components';
export declare function downloadGithubFolder(components: Pick<CliComponents, 'fs' | 'logger' | 'fetch'>, githubUrl: string, destination: string): Promise<void>;
