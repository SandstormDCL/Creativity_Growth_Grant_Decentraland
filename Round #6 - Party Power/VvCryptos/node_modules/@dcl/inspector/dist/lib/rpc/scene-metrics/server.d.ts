import { RPC, Transport } from '@dcl/mini-rpc';
import { RootState } from '../../../redux/store';
import { SceneMetricsRPC } from './types';
export declare class SceneMetricsServer extends RPC<SceneMetricsRPC.Method, SceneMetricsRPC.Params, SceneMetricsRPC.Result> {
    constructor(transport: Transport, store: {
        getState: () => RootState;
    });
}
