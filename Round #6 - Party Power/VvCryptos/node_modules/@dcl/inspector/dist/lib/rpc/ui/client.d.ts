import { RPC, Transport } from '@dcl/mini-rpc';
import { UiRPC } from './types';
import { AssetsTab, PanelName, SceneInspectorTab } from '../../../redux/ui/types';
export declare class UiClient extends RPC<UiRPC.Method, UiRPC.Params, UiRPC.Result> {
    constructor(transport: Transport);
    toggleComponent: (component: string, enabled: boolean) => Promise<void>;
    togglePanel: (panel: `${PanelName}`, enabled: boolean) => Promise<void>;
    toggleGizmos: (enabled: boolean) => Promise<void>;
    toggleGroundGrid: (enabled: boolean) => Promise<void>;
    selectAssetsTab: (tab: `${AssetsTab}`) => Promise<void>;
    selectSceneInspectorTab: (tab: `${SceneInspectorTab}`) => Promise<void>;
    toggleSceneInspectorTab: (tab: `${SceneInspectorTab}`, enabled: boolean) => Promise<void>;
}
