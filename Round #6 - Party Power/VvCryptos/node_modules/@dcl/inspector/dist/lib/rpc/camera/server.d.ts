import { Engine, FreeCamera } from '@babylonjs/core';
import { RPC, Transport } from '@dcl/mini-rpc';
import { CameraRPC } from './types';
export declare class CameraServer extends RPC<CameraRPC.Method, CameraRPC.Params, CameraRPC.Result> {
    constructor(transport: Transport, engine: Engine, camera: FreeCamera);
}
