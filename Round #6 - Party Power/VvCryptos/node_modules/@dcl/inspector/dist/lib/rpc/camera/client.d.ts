import { RPC, Transport } from '@dcl/mini-rpc';
import { CameraRPC } from './types';
export declare class CameraClient extends RPC<CameraRPC.Method, CameraRPC.Params, CameraRPC.Result> {
    constructor(transport: Transport);
    takeScreenshot: (width: number, height: number, precision?: number) => Promise<string>;
    setTarget: (x: number, y: number, z: number) => Promise<void>;
    setPosition: (x: number, y: number, z: number) => Promise<void>;
}
