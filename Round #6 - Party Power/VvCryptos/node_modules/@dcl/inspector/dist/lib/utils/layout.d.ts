import { Vector3 } from '@babylonjs/core';
export type Coords = {
    x: number;
    y: number;
};
export type Layout = {
    base: Coords;
    parcels: Coords[];
};
export declare function inBounds(layout: Layout, position: Vector3): boolean;
