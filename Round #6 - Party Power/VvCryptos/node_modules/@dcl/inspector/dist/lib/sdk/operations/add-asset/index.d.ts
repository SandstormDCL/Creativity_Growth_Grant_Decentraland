import { Entity, IEngine, Vector3Type } from '@dcl/ecs';
import { EnumEntity } from '../../enum-entity';
import { AssetData } from '../../../logic/catalog';
export declare function addAsset(engine: IEngine): (parent: Entity, src: string, name: string, position: Vector3Type, base: string, enumEntityId: EnumEntity, composite?: AssetData["composite"], assetId?: string) => Entity;
export default addAsset;
