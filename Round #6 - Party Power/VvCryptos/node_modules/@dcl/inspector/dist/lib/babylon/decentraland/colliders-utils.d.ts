import { AbstractMesh } from '@babylonjs/core';
import { GridMaterial } from '@babylonjs/materials';
import * as BABYLON from '@babylonjs/core';
export declare const colliderMaterial: (a: BABYLON.Scene, ...params: never[]) => GridMaterial;
export declare function markAsCollider(mesh: AbstractMesh): void;
export declare function isCollider(mesh: AbstractMesh): boolean;
