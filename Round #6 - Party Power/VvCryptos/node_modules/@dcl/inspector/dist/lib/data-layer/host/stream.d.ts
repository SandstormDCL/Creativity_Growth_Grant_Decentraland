import { DataLayerContext } from '../types';
export declare function stream(stream: AsyncIterable<{
    data: Uint8Array;
}>, ctx: Omit<DataLayerContext, 'fs'>, addUndoCrdt: () => void): AsyncGenerator<{
    data: Uint8Array;
}>;
