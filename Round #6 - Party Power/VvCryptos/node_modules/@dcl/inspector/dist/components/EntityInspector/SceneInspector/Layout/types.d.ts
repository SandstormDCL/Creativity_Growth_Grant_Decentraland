import { Layout } from '../../../../lib/utils/layout';
export type Props = {
    value: Layout;
    onChange: (value: Layout) => void;
};
export declare enum Mode {
    GRID = 0,
    ADVANCED = 1
}
export declare enum GridError {
    NOT_CONNECTED = 1,// start from 1 so we can use GridError as boolean type
    NUMBER_OF_PARCELS = 2,
    MISSING_BASE_PARCEL = 3
}
export declare const MAX_AXIS_PARCELS = 32;
export declare const TILE_OPTIONS: {
    value: number;
}[];
