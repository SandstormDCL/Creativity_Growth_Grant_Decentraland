import { ItemParams } from 'react-contexify';
/**
 * Used to get the functions to work with a Context Menu
 * @returns
 */
export declare const useContextMenu: () => {
    handleAction: (cb: (...params: any) => void, ...params: any) => ({ id, event }: ItemParams) => void;
};
