export type ContextMenuProps<T> = T & {
    contextMenuId: string;
};
/**
 * This can be used to wrap a component with a higher order component that will initialize a Context Menu
 * @param Component
 * @returns
 */
export declare function withContextMenu<P extends object>(Component: React.ComponentType<ContextMenuProps<P>>): React.FC<Omit<P, 'contextMenuId'>>;
