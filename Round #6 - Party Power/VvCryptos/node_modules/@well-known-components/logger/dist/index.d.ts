import { ILoggerComponent } from "@well-known-components/interfaces";
import { LoggerComponents } from "./helpers";
import { metricDeclarations } from "./metrics";
export { metricDeclarations };
/**
 * Creates a scoped logger component that selects a readable output for tests
 * and json logger for NODE_ENV=production
 * @public
 */
export declare function createLogComponent(components: LoggerComponents): Promise<ILoggerComponent>;
/**
 * Creates a scoped logger component to print a readable output to the stderr
 * @public
 */
export declare function createConsoleLogComponent(components: LoggerComponents): Promise<ILoggerComponent>;
/**
 * Creates a scoped logger component to print JSON to the stderr.
 * Useful for cloudwatch and other logging services.
 * @public
 */
export declare function createJsonLogComponent(components: LoggerComponents): Promise<ILoggerComponent>;
//# sourceMappingURL=index.d.ts.map