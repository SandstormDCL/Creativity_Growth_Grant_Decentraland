import {
  engine,
  Transform,
  inputSystem,
  PointerEvents,
  InputAction,
  PointerEventType,
  Material,
} from '@dcl/sdk/ecs'
import { Color4, Quaternion, Vector3 } from '@dcl/sdk/math'
import { Cube, Spinner } from './components'
import { getRandomHexColor } from './utils'


export function circularSystem(dt: number) {
  // Ejemplo: mover una entidad en un círculo
}

export function changeColorSystem(dt: number) {
  // Ejemplo: cambiar el color de los cubos
}